name: Create demo issue

on:
  push:

jobs:
  msg:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Simple demo for for resuable workflow"

  issue:
    uses: rndrepos/common/.github/workflows/create-repo-issue.yml@main
    with:
      title: 'Test issue'
      body: 'Test body'
    secrets:
      token: ${{ secrets.PAT }}
